fastapi
uvicorn[standard]
boto3
mlflow
torch
numpy
pandas
scikit-learn
pydantic-settings
python-multipart

# 모델 변환 의존성(coremltools / ai-edge-torch)은 TF·JAX·CUDA 스택을 동반해 매우 무겁다.
# CI·웹에는 불필요(테스트는 가짜 주입)하므로 requirements-convert.txt 로 분리.
# 변환을 실제 수행하는 워커 환경에서만:  pip install -r requirements.txt -r requirements-convert.txt
