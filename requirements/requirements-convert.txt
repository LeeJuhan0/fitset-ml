# 모델 변환 전용 의존성 — 변환을 실제 수행하는 환경에만 설치한다.
#   pip install -r requirements.txt -r requirements-convert.txt
# 미설치 시 trainer는 ImportError를 잡아 convert_warning 태그만 남기고 학습은 완료된다.

# iOS — CoreML (비교적 가벼움 → 단일 박스에서 동작 가능)
coremltools

# Android — ONNX (torch.onnx.export가 요구). 가벼워서 t3.small에서 동작.
# 구 TFLite 경로(ai-edge-torch)는 TF 스택 동반으로 t3.small에서 못 돌아 폐기 — ONNX Runtime으로 대체.
onnx
