# CPU 전용 torch — EC2(t3.small)·CI엔 GPU가 없어 CUDA 휠(nvidia-*, 수 GB)은
# 낭비이자 디스크 폭발(No space left on device)의 주범. CPU 빌드로 이미지 대폭 축소.
--extra-index-url https://download.pytorch.org/whl/cpu

fastapi
uvicorn[standard]
boto3
mlflow
torch==2.12.1+cpu
numpy
pandas
scikit-learn
pydantic-settings
python-multipart
httpx          # MLflow UI 역프록시(app/mlflow_proxy.py)의 중계 클라이언트
pyjwt[crypto]  # 유저 JWT RS256 검증(app/core/auth.py) — crypto = RSA 서명 검증용 cryptography 동반

# 모델 변환 의존성(coremltools / ai-edge-torch)은 TF·JAX·CUDA 스택을 동반해 매우 무겁다.
# CI·웹에는 불필요(테스트는 가짜 주입)하므로 requirements-convert.txt 로 분리.
# 변환을 실제 수행하는 워커 환경에서만:  pip install -r requirements.txt -r requirements-convert.txt
